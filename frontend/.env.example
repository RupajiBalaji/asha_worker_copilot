# Copy to .env.local for local development overrides
# Vite only exposes vars prefixed with VITE_ to the browser

# Backend API base URL — set this to your Render backend URL for production
# Leave blank for local dev (defaults to http://localhost:8000)
VITE_API_BASE=https://asha-copilot-backend.onrender.com
